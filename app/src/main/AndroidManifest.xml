<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practicepm">
    <!-- StartApp required permissions integration -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- End of StartApp permissions integration -->
    <!-- StartApp optional permissions integration -->
    <!-- Note: StartApp SDK doesn't request location updates proactively but only uses the last known location. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityboris"></activity>
        <activity android:name=".MainActivityAttlee" />
        <activity android:name=".MainActivityChurchill" />
        <activity android:name=".MainActivityEden" />
        <activity android:name=".MainActivityMacmillian" />
        <activity android:name=".MainActivityHome" />
        <activity android:name=".MainActivityHeath" />
        <activity android:name=".MainActivityWilson" />
        <activity android:name=".MainActivityCallagan" />
        <activity android:name=".MainActivityThatcher" />
        <activity android:name=".MainActivityMajor" />
        <activity android:name=".MainActivityBlair" />
        <activity android:name=".MainActivityBrown" />
        <activity android:name=".Main3Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>